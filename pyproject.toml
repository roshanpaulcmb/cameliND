[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cameliND"
version = "0.1.0"
description = "Camelid nanobody design: OpenMM MD simulation and binding analysis"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
